require "scripts.out"

function init(self)
	self.script = MainUI()
	self.script:AssignProperties(self)
	support.Component.Register(self.script.Locator, self.script, MainUI)
	self.script:init()
end

function on_message(self, message, sender)
	self.script:on_message(message, sender)
end

